import { createStyles } from '@mantine/core';

const useHomeStyles = createStyles(() => ({
  root: {
    height: '100%',
  },
  allGridRoot: {
    width: '40%',
  },
}));

export default useHomeStyles;
