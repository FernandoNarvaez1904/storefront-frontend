import { createStyles } from '@mantine/core';

const useItemsStyles = createStyles((theme) => ({
  itemsPageContainer: {
    height: '100%',
    padding: theme.spacing.xs,
  },
}));

export default useItemsStyles;
